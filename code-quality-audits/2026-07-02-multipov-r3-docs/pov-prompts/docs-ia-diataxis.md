ROUND 3 multi-POV review of `@ponchia/ui` (bronto-ui). Theme: DOCUMENTATION & IA. Rounds 1
(broad docs-product) and 2 (repo navigation) are done — go DEEP on CONSUMER-FACING docs.
READ-ONLY.

Your lens: **DOCUMENTATION INFORMATION ARCHITECTURE (Diátaxis, findability, cross-linking).**
Judge the docs as a navigable system for a consumer (NOT repo-contributor navigation):
- Diátaxis balance: does the doc set cleanly separate TUTORIAL (learning-oriented) vs HOW-TO
  (task-oriented) vs REFERENCE (information-oriented) vs EXPLANATION (understanding-oriented)?
  Map the ~57 docs/ files to those modes — what's missing or conflated? (e.g. is there a real
  tutorial, or only reference + how-tos?)
- The docs viewer (docs/index.html) + docs/README.md inventory: is the categorization coherent,
  is everything findable in ≤2 hops, is there dead/duplicated navigation, and does search/ToC
  exist? Is the ordering principled?
- Cross-linking: do related docs link to each other (primitive → its tokens → theming →
  contrast)? Orphan pages with no inbound links? A clear "next step" from each page?
- Naming/terminology consistency across docs (e.g. legend vs legends, identity-layer vs
  standard-set) — does the vocabulary stay stable so search works?
- Progressive disclosure: can a reader go from overview → concept → task → reference without
  whiplash? Or must they already know the mental model to navigate?
- The 40+ per-primitive docs: are they a coherent reference section or a flat undifferentiated pile?

--- OUTPUT CONTRACT ---
One report on a multi-POV DOCUMENTATION & IA panel; a synthesizer merges it. Ground EVERY claim
in a path you opened. Produce your report as your FINAL ANSWER in EXACTLY this structure:

# Documentation IA & Diátaxis — bronto-ui documentation review
**Verdict:** one paragraph.
**Grade:** A–F + half-sentence why.
## Strengths
- bullets citing path.
## Weaknesses / risks
- bullets; prefix [P0]/[P1]/[P2]/[P3]; cite path; say the findability/comprehension cost.
## Top recommendations
1..6 ranked; tag each safe-mechanical vs needs-writing.
## Notable observations
- surprising / worth the synthesizer knowing.
Evidence-dense, no filler, no restating this prompt.
